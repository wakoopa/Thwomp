#!/bin/sh
echo Installing homebrew and required libs
/usr/bin/ruby -e "$(curl -fsSL https://raw.github.com/gist/323731)"

brew update && brew install giflib boost libagg ffmpeg gstreamer

#export CC=gcc-4.2
#echo Installing ffmpeg
#cd /tmp/
#wget http://ffmpeg.org/releases/ffmpeg-0.8.1.tar.gz
#tar -xzf ffmpeg-0.8.1.tar.gz
#cd ffmpeg-0.8.1
#./configure
#make
#make install

echo Installing gnash

cd /tmp/
wget ftp://ftp.mirror.nl/pub/mirror/gnu/gnash/0.8.9/gnash-0.8.9.tar.gz
tar -xzf gnash-0.8.9.tar.gz
cd gnash-0.8.9
./configure --enable-renderer=agg --enable-gui=dump --enable-media=ffmpeg
make
make install

echo Done?!
